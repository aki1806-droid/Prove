[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pickyassist"
version = "0.1.0"
description = "Client Python non ufficiale per le API v2 di Picky Assist"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
packages = ["pickyassist"]
